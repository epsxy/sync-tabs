export class Todo {
    id: string;
    content: string;
    checked: boolean;

    constructor(id: string, content: string, checked: boolean) {
        this.id = id;
        this.content = name;
        this.checked = checked;
    }
}
